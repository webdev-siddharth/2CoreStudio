/**
 * ============================================================================
 * GENERATED TYPES PLACEHOLDER — DO NOT HAND-WRITE.
 * ============================================================================
 *
 * These types are generated from the live Supabase schema. Once your project
 * is linked, run:
 *
 *   supabase login
 *   supabase link --project-ref <your-project-ref>
 *   supabase gen types typescript --project-id <your-project-ref> > src/types/database.types.ts
 *
 * Until then, code uses the lightweight interfaces in src/lib/types.ts.
 * The generated file exports a `Database` interface consumed by
 * createClient/createServerClient (e.g. createClient<Database>(...)).
 */
export type Database = never;
